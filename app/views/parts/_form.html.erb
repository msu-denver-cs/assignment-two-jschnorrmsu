<%= form_with(model: part, local: true) do |form| %>
  <% if part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(part.errors.count, "error") %> prohibited this part from being saved:</h2>

      <ul>
      <% part.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :car_type %>
    <%= form.text_field :car_type %>
  </div>

  <div class="field">
    <%= form.label :cars_available %>
    <%= form.text_field :cars_available %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
